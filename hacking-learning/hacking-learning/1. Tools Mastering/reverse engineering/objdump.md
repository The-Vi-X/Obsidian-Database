`objdump -h binary` - выводит данные хранимые в бинарнике
`objdump -R binary` - output GOT binary address

`.bss` - показывает адреса не инициализированных глобальных переменных
`.data` - показывает адреса инициализированных глобальных переменных